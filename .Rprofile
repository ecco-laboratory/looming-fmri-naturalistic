source("renv/activate.R")

# Global-ish variables used in the targets pipelines
path_here_fmri <- c("ignore", "data", "fmri")
path_here_derivatives <- c(path_here_fmri, "derivatives", "fmriprep-23.1.4")
# for copies of derivatives being written to a separate folder so as not to conflict with the original/continuing SPLaT targets pipeline
path_here_derivatives_local <- c("ignore", "data", "derivatives_local")
path_here_beh_local <- c("ignore", "data", "beh_local")
# the conda path points directly to the python binary associated with the conda environment you should get
# when you create from environment file
# but you will still need to change this path!
conda_path <- "/home/data/eccolab/SPLaT_fMRI_ms1/env/bin"
matlab_path <- "/opt/MATLAB/R2024a/bin"
